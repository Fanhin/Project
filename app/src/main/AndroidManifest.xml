<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tripbuddyv2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity android:name=".BottomNav.ActivityActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".BottomNav.LodgingEditActivity" android:parentActivityName=".MainActivity" />
        <activity android:name=".BottomNav.TransportationActivity" />
        <activity android:name=".Tab.TabActivity"/>
        <activity android:name=".TransportationSpinner"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".AddTripActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".BottomNav.AlertReceiver"/>
    </application>

</manifest>